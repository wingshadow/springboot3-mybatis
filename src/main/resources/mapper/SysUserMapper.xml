<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hawk.admin.orm.dao.SysUserMapper">
    <!--根据用户id查询用户角色信息-->
    <select id="selectRoleByUserId" resultType="com.hawk.common.core.domain.entity.SysRole">
        SELECT sr.*
        FROM sys_role sr,
             sys_role_user sur
        WHERE sr.role_id = sur.role_id
          AND sur.user_id = #{userId}
    </select>

    <select id="getAllUser" resultType="com.hawk.common.core.domain.entity.SysUser">
        SELECT u.*
        FROM sys_user u
                 LEFT JOIN sys_dept d ON u.dept_id = d.dept_id
        WHERE u.del_flag = '0'
            ${params.data_scope}
    </select>
</mapper>
