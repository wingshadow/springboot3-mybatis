<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hawk.admin.orm.mapper.BizCarInfoMapper">
    <!--查询某个用户根据的用户角色能够查看的部门
        根据部门表中的data_scope字段的不同值即对应不同的查询语句，将动态语句放入map中即可。
        1：全部数据权限 2：自定数据权限 3：本部门数据权限 4：本部门及以下数据权限
    -->
    <select id="getCarInfoList" resultType="com.hawk.admin.orm.entity.BizCarInfo">
        SELECT c.*
        FROM biz_car_info c
        left join sys_dept d on d.dept_id = c.dept_id
        where 1=1
        <if test="carNum!=null">
            and c.car_num = #{carNum}
        </if>
        ${params.data_scope}
    </select>


    <select id="getCarInfoPage" resultType="com.hawk.admin.orm.entity.BizCarInfo">
        SELECT c.*
        FROM biz_car_info c
        left join sys_dept d on d.dept_id = c.dept_id
        where 1=1
        ${params.data_scope}
<!--        ${ew.getCustomSqlSegment}-->
    </select>

    <select id="getCarInfoPage2" resultType="com.hawk.admin.orm.entity.BizCarInfo">
        SELECT c.*
        FROM biz_car_info c
        left join sys_dept d on d.dept_id = c.dept_id
                ${ew.getCustomSqlSegment}
    </select>

</mapper>
